{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "ignoreCommand": "if git cat-file -e ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^} 2>/dev/null; then git diff --quiet ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^} HEAD .; else exit 1; fi"
}
