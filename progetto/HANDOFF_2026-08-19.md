# Passaggio di consegne — Metabole, 19/8/2026

Da incollare in cima a una chat nuova. **Sostituisce** `HANDOFF_2026-08-18.md` per quello che è stato
chiuso oggi; tutto il resto di quel foglio (le decisioni aperte, i numeri, il filo delle porzioni)
**resta valido** ed è richiamato qui sotto senza essere ricopiato. Tutto quello che c'è qui è
**verificato nel codice o nei dati**, non ricordato.

⚠️ `PUNTO_DELLA_SITUAZIONE.md` è fermo all'11/8 e `DA_FARE.md` è chiuso: non si decide da lì. Lo stato
vivo sono `progetto/REGISTRO.md` (la sezione in cima) e `backend/src/lavori/voci-iniziali.ts` (le voci
**senza** `fatta: true`).

---

## 0. Come si lavora qui (queste regole vengono prima di tutto)

Valgono quelle del §0 del 18/8, **più tre cose imparate oggi**:

- ⚠️ **La revisione va fatta anche su APP e BACKOFFICE, non solo sul backend.** I tre punti peggiori
  di oggi — compreso quello che scriveva «Non hai ancora un piano attivo» a chi aveva appena pagato —
  stavano nell'app, e sono usciti solo alla **quarta** ronda, quando ho pensato a cercarli lì. Un
  valore nuovo che esce dal server arriva a un'app che si aggiorna **dopo**.
- ⚠️ **«Le letture sono già a posto» non è una verifica.** Il 18/8 avevo adeguato una parte dei punti
  che leggono `subscription.status` e dato per scontato il resto; oggi la revisione avversariale ne ha
  trovati **dodici** ancora sbagliati, e il peggiore scriveva a una cliente che aveva appena pagato
  «il tuo piano è terminato». Un censimento si rifà col `grep`, non con la memoria di ieri.
- ⚠️ **Ogni revisione trova difetti INTRODOTTI da quella prima.** È successo alla seconda, alla terza
  e alla quarta, ed era successo anche il 18/8. Una correzione che allarga un filtro fa entrare righe
  dove prima non entravano: la ronda dopo va fatta **su quello che si è appena corretto**.
- ⚠️ **Un doppio di comodo che si comporta diversamente dall'originale non verifica niente.** Metà dei
  test nuovi di oggi passavano identici sul codice sbagliato, perché il finto Prisma non filtrava per
  stato. Dove il test guarda una `where`, il commento lo dice; dove può, il finto **filtra come il
  database vero**.

---

## 1. Cosa gira adesso

- `origin/main` = **`ce6a657`** («Le stelle mai date non orientano più il motore»), pushato nella
  notte fra il 18 e il 19.
- Backend: **229 suite / 3590 test verdi**, build pulita. Nessuna migrazione nuova oggi.
- Fotografia delle code (`npm run diag:coda`, 19/8 mattina): 15 righe con un piano, **4 in coda,
  tutte nella forma vecchia**, **0 clienti con due piani che erogano insieme**. Le 4 partono il 22/8
  (due) e il 31/8 (due).
- ⚠️ Le modifiche lato app del 18/8 (porzioni scalate, cosce, popup valutazioni, base certificata)
  arrivano alle clienti **solo con la prossima pubblicazione o OTA 2.1.8**.

## 2. ⛔ Due consegne sono scritte e NON committate

1. **«Il piano in coda si scrive `queued` — e le dodici letture che non lo capivano»** (voce 258,
   seconda metà). Messaggio in `progetto/COMMIT_parte_coda_scrittura.txt`.
2. **«L'attore che non esiste»** — di **tutti i pagamenti con carta** non restava una riga di
   registro, ed è un difetto più vecchio della voce 258 (§3). Messaggio in
   `progetto/COMMIT_parte_attore_che_non_esiste.txt`.

`progetto/COMMIT.txt` ha già le righe di tutte e due; si possono fare in un commit solo o in due.

### ⚠️ L'ordine del rilascio, che qui non è un dettaglio

L'app si aggiorna **dopo** il backend (pubblicazione o OTA). Tre correzioni di questa consegna sono
**lato app** — «Non hai ancora un piano attivo» nel Profilo di chi ha appena pagato, la parola grezza
«queued» nel Calendario, e lo spostamento della data d'inizio che spariva proprio dai piani
spostabili — e finché l'app non è pubblicata quelle schermate restano com'erano.

1. Deploy del **backend** (da qui in poi i piani nuovi in coda nascono `queued`).
2. **Pubblicazione o OTA dell'app.** ⚠️ Fra 1 e 2 c'è una finestra in cui una cliente che compra con
   partenza futura vede il Profilo sbagliato: sono i casi nuovi, e oggi sono pochi — ma sono quelli
   che hanno appena pagato.
3. **Solo dopo**: `npm run converti:code` (prova a vuoto, poi `CONFERMA=1`) per le 4 code vecchie.
   ⚠️ Lo script per ultimo anche per un secondo motivo: converte righe che il codice **vecchio** cerca
   come `active`. E le 4 clienti che tocca sono già in coda: se l'app non è ancora aggiornata,
   lanciarlo vuol dire far comparire a loro il Profilo sbagliato.

---

## 3. Cosa è stato chiuso oggi

**La scrittura.** `finalizeApproval` scrive `queued` quando il piano comincia più avanti;
`promuoviCodeArrivate` è il **primo passo** del `daily` e fa partire le code arrivate.

**Le tredici letture** che confrontavano ancora `status === 'active'` a mano: menu (schermata ed
erogazione), calcolo della coda, abbonamento Stripe (profilo, disdetta, ripensamento), referral,
pausa, scheda cliente, diagnostiche, vetrina del Monitoraggio, contatori di analytics e coach-tasks,
check-in, messaggio quotidiano, avviso di sovrapposizione, racconto dell'annullamento, e — trovata
solo alla **quarta** ronda — il **sollecito delle misure**, che lasciava la cliente in coda con l'app
ferma sulla schermata «servono le tue misure» senza mandarle nessuna richiesta.

**E tre punti dell'APP e del backoffice**: il Profilo scriveva «Non hai ancora un piano attivo» a chi
aveva appena pagato (⚠️ e anche a chi ha un piano in corso e ne compra uno in coda, perché
`/me/subscription` risponde con il più recente), il Calendario stampava la parola inglese «queued» in
una pastiglia verde, e la matita del backoffice rispondeva «stato: queued» in italiano.

**I cinque punti che scrivono la data d'inizio** ora chiamano `statoPerInizio`: una domanda, una
risposta.

**Tre difetti più vecchi della voce, e uno che stavo per introdurre io**, trovati per strada e chiusi:

- ⚠️ **L'audit di tutti i pagamenti con carta non si scriveva** (seconda consegna del §2):
  `AuditLog.actorId` è una chiave esterna su `user`, e `'stripe-webhook'` — come `'public'` sul lead
  dal sito, come il mio `'system'` — non è un utente. L'INSERT falliva, l'errore veniva assorbito
  (giustamente) e la riga si perdeva **in silenzio**. Adesso si riprova una volta senza attore,
  tenendo nel `metadata` chi diceva di essere. ⚠️ Le righe perse **non tornano**.
- lo stesso difetto stava per entrare nella promozione notturna con `actorId: 'system'`;
- chi comprava il **rinnovo in anticipo smetteva di ricevere menu** (la finestra si misurava su
  `planStartDate`, che l'acquisto in coda riallinea al piano **nuovo**): in produzione dal 10/8;
- la data scelta **dopo il pagamento** da una cliente di ritorno non muoveva l'abbonamento.

---

## 4. Aperto

### 4.1 In coda a Simone — produzione

1. ⛔ **Committare e pushare** la consegna del §2.
2. Dopo il deploy: `npm run converti:code` (prova a vuoto), poi `CONFERMA=1 npm run converti:code`.
3. Dopo il deploy: **«Aggiorna dal rilascio»** in pagina Lavori (o `CONFERMA=1 npm run carica:lavori`).
4. Shell di Render: **`CONFERMA=1 npm run importa:ig`** — l'anteprima del 19/8 dice **96 righe già
   complete, 0 da scrivere**: la tabella del capo nutrizionista è già tutta dentro. ⚠️ Vuol dire che
   quel passo **si può anche saltare**: non cambierebbe niente.
5. **Allergie** (`npm run conta:allergie`, lanciato il 19/8): 50 clienti, **21 da ricontattare** — 3
   con allergie in testo libero da codificare (Favismo · «Carboidrati» · un elenco lungo) e 18 che non
   hanno **mai risposto** alla domanda. Nessuna intolleranza «Altro» rimasta ignota. ⛔ È il numero che
   sbloccava il §7 del foglio di ieri: adesso c'è.
6. Il permesso **`cancel_subscription` a Lucia** in pagina Permessi (in coda dal 17/8, da confermare).
7. ⛔ **Repository pubblico**: privato o storico riscritto. È una scelta sua.

### 4.2 Decisioni che aspettano una parola di Simone

**Nuove, da oggi:**

- ⭐ **Le code arrivate a scadenza senza mai partire.** Oggi in produzione non ce n'è nessuna. Se un
  giorno ce ne fosse una, `promuoviCodeArrivate` **non la promuove** — da attiva-e-finita prenderebbe,
  nella stessa notte, il report di fine percorso, la chiusura CRM e (sulle prove) la cancellazione
  della personalizzazione, e nessuna delle tre si torna indietro. Resta `queued`, si grida nei log e
  compare in `npm run diag:coda`. **Cosa farne — rimborso, partenza posticipata, piano nuovo — è una
  decisione, una per una.**
- **Una pausa chiesta prima che il piano cominci.** Adesso non allunga niente e lo scrive nei log:
  non c'era nessun menu da sospendere, e regalare giorni sarebbe un numero inventato sulla scadenza.
  Ma la domanda vera è un'altra: quella pausa dovrebbe **spostare l'inizio**? Oppure va rifiutata
  dicendoglielo?

**Restano dal 18/8, invariate** (§4.2 di quel foglio): la seconda taglia di catalogo contro il tetto
del moltiplicatore ⭐ · la coda «Da validare» C · chi **chiede** alla cliente cosa intendeva con
«tranne» · rilascio e testi corretti a mano · l'arrotondamento dei **pezzi** · le **cinque** di
`DECISIONE_Due_Schermate_App.md` · le conferme di Lucia su dolce/salato · i nomi dello staff nei
registri se il repository resta pubblico.

### 4.3 Codice ancora da scrivere

- **Il vincolo in banca dati** sui piani sovrapposti (voce `coda-vincolo-e-code-scadute`). Adesso si
  può: lo stato vive e le sovrapposizioni sono **zero**. ⚠️ Va per ultimo di proposito — un vincolo
  aggiunto insieme alla scrittura trasforma un dato storto in un errore 500 su una cassa.
- **App, i due dati grossi**: l'analisi è fatta (`DECISIONE_Due_Schermate_App.md`) e non va rifatta;
  servono le cinque decisioni.
- **Ordine del menu**: resta solo il difetto 6, aperto di proposito.
- **Porzioni** (voce 255): il codice è finito, resta la decisione sui pezzi.

### 4.4 Due punti lasciati senza test dedicato — ✅ **chiusi in giornata**

Erano `coach-tasks` sui compiti G0…G7 della prova e `signals` sull'evento `trial_measures_ok`. Sono
scritti qui perché un giorno qualcuno li avrebbe riletti chiedendosi se erano coperti: adesso lo
sono (4 test), insieme all'ultima incoerenza rimasta — le **scadenze in arrivo**, che la dashboard
della coach e l'appunto in Calendario contavano in due modi diversi per lo stesso evento.

---

## 5. I numeri di oggi

| cosa | numero |
|---|---|
| test backend | **3590** in **229 suite** · app **106** · backoffice **25** |
| letture sbagliate trovate dalle revisioni | **13** (più 3 difetti più vecchi della voce) |
| punti che scrivono la data d'inizio | **5**, ora tutti su `statoPerInizio` |
| code in produzione | **4**, tutte in forma vecchia · **0** sovrapposizioni |
| clienti da ricontattare per le allergie | **21** su 50 (3 da codificare, 18 che non hanno risposto) |
| righe IG già complete in produzione | **96 su 96** (l'import non ha niente da scrivere) |
| ronde di revisione avversariale | **4** · mutation testing: **2** |

## 6. Il filo della giornata, se serve capirlo in una riga

La voce era «scrivere `queued`», ed è la parte che ha richiesto meno tempo. Quello che ha richiesto la
giornata è stato scoprire che **la sequenza prudente del 18/8 — prima le letture, poi la scrittura —
era stata rispettata solo a metà**, e che fra le letture rimaste indietro c'era quella che dice a una
cliente se ha ancora un piano. La regola che ne esce non è «rifare i censimenti»: è che *«l'ho già
fatto ieri»* non è una verifica, e che il costo di riguardare è sempre più basso del costo di una
frase sbagliata sullo schermo di chi ha appena pagato.
