# Metabole — Registro Sviluppo (solo Cowork)

Log delle modifiche di sviluppo fatte da Simone + Claude Cowork. Tenuto **separato** da `REGISTRO.md` (che il socio aggiorna) per evitare conflitti di merge. Si aggiunge in cima.

---

## 2026-07-15


- **Motore R11/R12 — rifiniture: segnalazioni coach instradate + soglie in UI** — richiesta di Simone ("facciamo le rifiniture R11/R12"). **R11 (scoring per obiettivo): già fatto** — la modulazione "in mantenimento l'efficacia è ridotta/neutra, niente spinta al deficit" era già in `buildScoringContext` (`menu_maintenance_w_eff`, applicato anche sopra gli stati che boosterebbero l'efficacia). Nessuna modifica. **R12 (segnalazioni): implementate le due mancanti**, aperte col routing categorizzato (`EscalationRoutingService`, che assegna al ruolo già assegnato alla cliente, con dedup): **(1) "Nessun progresso"** → in `signals.service.checkNoProgress`, alla registrazione misura, se la cliente è in stallo (`progress.getProgress().alerts.stalled`) apre una `no_progress` (→ nutrizionista, coach informata); gate config `no_progress_escalation` (bool, default OFF). **(2) "Scarsa aderenza"** → nuovo `signals.service.runAdherenceSweep()` agganciato al **cron giornaliero** (`cron.controller`/`cron.module` importano `SignalsModule`): per le clienti attive che avevano check-in ma non ne fanno da `low_adherence_days` giorni apre una `low_adherence` (→ coach); gate config `low_adherence_days` (default 0 = spenta). **Wiring:** `SignalsModule` importa `EscalationsModule` (nessuna dipendenza circolare: EscalationsModule è foglia). **Soglie in UI:** aggiunti 4 parametri al catalogo motore (`engine-rules.catalog.ts`) sotto categoria **"sicurezza"** → visibili/modificabili in **Regole motore → Regole globali**: `stall_days_before_coach_alert`, `no_progress_escalation`, `low_adherence_days`, `max_weight_change_alert_kg_week`. Entrambe le segnalazioni sono **additive e OFF di default** (nessun effetto finché il capo non imposta le soglie → rispetta il "va deciso col socio"). Type-check backend possibile via clone: 0 errori reali nei file toccati (solo artefatti del client Prisma stale, identici alla produzione).


- **Diete — nome pulito, rinomina dal backoffice, script pulizia lead/clienti** — tre richieste di Simone in fila. **(1) Nome rispettato:** la generazione appendeva "— bozza generata" al nome (`${preset.label} — bozza generata`) → ora la dieta prende **esattamente il nome inserito** (`engine-rules.service.ts`, `name: preset.label`); vale per le nuove generazioni. **(2) Rinomina da "Gestione dieta":** nuovo `PATCH /diets/:id/name` → `CatalogService.renameDiet` (cambia solo l'etichetta, **consentito anche su diete approvate**, non azzera lo stato né tocca i menu) + `RenameDietDto`; in `GestioneDieta.tsx`, selezionando una dieta compare un campo **Nome** precompilato + pulsante **Rinomina** → così le diete col vecchio suffisso si sistemano dal backoffice in due click. Type-check frontend 0 errori, backend (catalog) pulito nel filtro. **(3) Cancellazione mirata lead + clienti:** nuovo `backend/prisma/cleanup-clients.ts` (+ `npm run cleanup:clients`), modellato sullo `cleanup:demo` collaudato ma che **TIENE il catalogo diete/ricette e i costi di contabilità**: cancella schede CRM (lead), profili cliente, utenti-cliente e tutto il collegato (menu, misure, check-in, chat, acquisti/abbonamenti, provvigioni/compensi/ledger derivati), svuota notifiche/token/analytics. Anteprima di default; cancella solo con `METABOLE_CLEANUP_CONFIRM=SI-CANCELLA`, in transazione unica (rollback totale se un vincolo blocca). Aggiunto anche `fix-diet-names.ts` (+ `npm run fix:diet-names`, conferma `METABOLE_FIX_CONFIRM=SI`) per rinominare in blocco le diete togliendo il suffisso. **NB:** gli script girano sul Render Shell (client Prisma generato); servono commit + deploy prima. ⚠️ La cancellazione è irreversibile: backup/branch del DB Neon prima.


- **Motore/AI — generazione catalogo robusta (basta errori "JSON at position…")** — su output grandi l'AI (haiku) restituiva JSON malformato in modi sempre diversi (virgola mancante tra elementi, carattere di controllo non-escapato dentro una stringa, virgola finale, troncatura) → il parse falliva e la generazione si bloccava, su tutti gli stili (low carb, flessibile, keto…). **Fix in `AiService.generateJson`:** parsing tollerante con una **`repairJson`** che (1) escapa i caratteri di controllo dentro le stringhe, (2) inserisce le virgole mancanti fra token strutturali e rimuove quelle finali, (3) bilancia parentesi/graffe lasciate aperte (troncatura). **In `engine-rules.service.ts`:** la generazione ora **ritenta fino a 3 volte** (gli errori sono in punti diversi ogni volta → stocastici), output più piccolo (**5 ricette per pasto** invece di 6) e prompt più stringente ("JSON valido e minificato, ogni elemento separato da virgola, nessuna virgola finale"). Type-check backend possibile grazie ai node_modules del clone: 0 errori reali nei file toccati (gli unici residui sono artefatti del client Prisma non rigenerabile, identici alla versione già in produzione).

- **Permessi — pagine "hub": due voci di menu gestiscono tutto (richiesta di Simone)** — "vorrei fargli vedere solo quelle due pagine (Creazione e validazione + Gestione dieta) che da lì gestiscono tutto". Problema: quelle due pagine chiamano API di domini diversi (`diets_catalog`, `recipes`) e il `PageGuard` bloccava chi non aveva il permesso diretto su quei cataloghi ("Non hai il permesso per questa sezione"). **Fix:** nuova mappa **`PAGE_GRANTS`** in `permissions/pages.ts` (`diet_workspace` e `creation_validation` → concedono `diets_catalog` + `recipes`) e fallback nel `PageGuard`: se manca il permesso diretto sulla pagina richiesta, si controlla se l'utente ha una pagina-hub che la concede. I **gruppi di equivalenza** funzionano già col solo ruolo (nessun `@RequirePage`). Così, abilitando al nutrizionista SOLO "Creazione e validazione" + "Gestione dieta" (e disattivando Catalogo diete/Ricette/Gruppi/Allergeni nei Permessi), gli editor interni continuano a funzionare ma quelle voci spariscono dal menu. Verificato: la lista diete in Gestione dieta si carica; type-check 0 errori.

- **Diete — rifiniture flusso approvazione (403, spinner, redirect, scroll)** — completano il flusso "Creazione e validazione": (1) le azioni del capo `approve`/`publish`/`reject` (già `@Roles('head_nutritionist')`) portate a **`@RequirePage('diets_catalog', 'view')`** → non serve più il *manage*, sblocca "Approva e pubblica" quando la riga permessi del capo non ha il manage; (2) dopo la pubblicazione del responsabile **redirect a `/gestione-dieta`** (`useNavigate`); (3) **spinner animato** durante la generazione ("Genero…" + riga "può richiedere fino a un minuto", animazione `@keyframes spin` esistente, stile inline); (4) **scroll-to-top** automatico su esito/errore, così i banner (successo o errore) sono sempre visibili anche scrollati sull'anteprima. Type-check backoffice 0 errori.


- **Diete — flusso approvazione per ruolo: il responsabile approva e pubblica, i coach inviano in revisione** — richiesta di Simone ("se a generare e approvare la dieta è il responsabile non va inviata in revisione, va approvata e pubblicata; solo se sono i coach sotto va mandata al responsabile"). Nel wizard "Creazione e validazione" il pulsante finale è ora **condizionale al ruolo**: `head_nutritionist` vede **"Approva e pubblica"**, gli altri (nutrizionisti/coach) vedono "Invia in revisione". **Backend:** nuovo `POST /diets/:id/publish` + `CatalogService.publishDiet` (`draft/rejected/in_review → approved` in un colpo, **senza** il blocco anti-auto-approvazione di `approveDiet` — valido solo per il capo; per i coach resta `submit → approve` del capo con il divieto autore). **Frontend (`CreazioneValidazione.tsx`):** `isResponsabile = user.role === 'head_nutritionist'`, chiama `publish` o `submit` di conseguenza. Type-check backoffice **0 errori**.

- **Diete — fix blocco "Non hai il permesso" su approva/pubblica + redirect a Gestione dieta** — Simone: "premo approva e non succede nulla" → il banner (fuori schermo mentre scrollato) diceva *"Non hai il permesso per questa sezione"*. **Causa:** `DietsController` è `@RequirePage('diets_catalog')`, quindi il `PageGuard` chiede il permesso **manage** su quella pagina per il POST; nel DB la riga del capo risultava senza manage (pur essendo `manage:true` nei default), quindi bloccava. **Fix:** poiché `approve`/`publish`/`reject` sono già ristretti da `@Roles('head_nutritionist')` (il ruolo è il controllo reale), aggiunto sui tre handler l'override **`@RequirePage('diets_catalog', 'view')`** → non serve più il manage, la sicurezza non cambia. **Redirect:** dopo la pubblicazione del responsabile la pagina non si azzera più ma naviga a **`/gestione-dieta`** (`useNavigate`). **UX generazione:** aggiunto lo **spinner animato** nel pulsante "Genero…" + riga "Sto generando… può richiedere fino a un minuto" (animazione `@keyframes spin` esistente, stile inline) e **scroll-to-top** automatico su esito/errore così i banner sono sempre visibili. Type-check **0 errori**. ⚠️ La parte permessi è **backend** → serve il redeploy di Render perché il 403 sparisca. In alternativa il capo può riattivare "Catalogo diete → gestisci" dai Permessi, ma col fix non è necessario.


- **CRM — il cliente registrato in app compare SEMPRE in Gestione lead (no doppioni)** — su segnalazione di Simone ("un cliente che si registra in app deve vedersi comunque nella tabella gestione lead… tutti sono lead e solo alcuni clienti"). La registrazione app passava già da `/auth/register` → `ensureLead`, ma l'`upsert` per `clientId` **creava un doppione** se la persona era già un lead "puro" con quella email (e non salvava il nome). **Riscritto `CrmService.ensureLead(clientId, email, name?)`** (`backend/src/commerce/crm.service.ts`): se già collegato fa backfill soft di email/nome; **se esiste un lead puro con la stessa email lo COLLEGA** (setta `clientId`, niente duplicati); altrimenti crea il record `lead_in`. `register()` ora passa anche il nome. Agganciato **anche alla creazione cliente da backoffice** (`users.service.ts`: iniettato `CrmService` da `CommerceModule`, `ensureLead` per ruolo `client`). Coerente col modello "tutti sono lead, solo alcuni clienti" (badge Cliente solo con `stage=paid`). Backend non tsc-abile in sandbox: usati solo campi/where Prisma validi (`clientId` è `@unique`), nessuna nuova dipendenza di modulo (Users→Commerce già esistente per FinanceService).

- **Backoffice — paginazione tabelle (100 righe/pagina, avanti/indietro + scelta pagina)** — richiesta di Simone. Nuovo componente riutilizzabile in `components/ui.tsx`: hook **`usePagination(items, 100)`** (aggancia la pagina all'intervallo valido quando i filtri riducono le righe) + **`<Pager>`** (prima «, ‹ Prec, selettore "Pagina N di M", Succ ›, ultima »; mostra "da–a di totale"; **si nasconde da solo se c'è una sola pagina**, quindi innocuo sulle tabelle corte). Applicato a **16 tabelle-dati** (scelta di Simone: solo quelle che crescono): Lead, Clienti, Acquisti, Pagamenti, Compensi, Provvigioni, Contabilità (costi), Log attività, Log email, Utenti, Segnalazioni, Agenda, Buoni sconto, Modelli email, Ricette. Le tabelle di configurazione piccole (Ruoli, Permessi, Allergeni, Testimonianze, Regole motore, Gruppi equivalenza) lasciate invariate. Type-check backoffice **0 errori**; ogni file editato verificato per **hash identico** al device (tranne `ui.tsx`, riallineato alla versione validata).

- **Assegnazione lead — massiva alla coach + filtri per colonna e per etichetta** — richiesta di Simone ("la responsabile seleziona massivamente i lead e li assegna in un unico passaggio" + "ogni colonna deve essere un filtro e devo poter selezionare anche per etichetta"). **Backend:** `POST /crm/leads/assign-coach-bulk` + `LeadAssignmentService.assignCoachMany` (updateMany, tutti in stato *pending*, **una sola notifica** alla coach, audit `crm.lead.assign_bulk`). **CRM (`LeadsTable.tsx`):** checkbox per riga + "seleziona/deseleziona tutti i visibili" nell'intestazione; barra di assegnazione massiva ("N selezionati" → tendina coach → "Assegna N lead"); **riga di filtri per colonna** (Nome, Email/tel, Stato, Coach con "non assegnato", Nutrizionista con "non assegnato", Tipo persona Cliente/Storico/Lead), combinati con ricerca ed **etichetta/lista** → filtra per etichetta e "seleziona tutti" per assegnare in blocco quel gruppo; pulsante "Azzera filtri". Type-check **0 errori**.

- **Prodotti — periodo del piano ora capisce i giorni (`8d` = 8 giorni)** — Simone: "ho messo 8d perché volevo 8 giorni ma ha creato 244 giorni". Il calcolo della scadenza abbonamento leggeva il periodo **sempre come mesi** (`parseInt("8d")=8` → 8 mesi ≈ 244 giorni). **Fix in `backend/src/commerce/commerce.service.ts`:** nuova funzione pura **`subscriptionEnd(start, period)`** con suffissi **`d`=giorni, `w`=settimane, `m`=mesi, `y`=anni** (senza suffisso = mesi, retrocompatibile; fallback prudente 3 mesi). Etichetta del campo aggiornata a **"Periodo (es. 8d, 2w, 3m, 1y)"** (`GestioneNegozio.tsx`). Verificata la logica su più casi (8d→8, 2w→14, 3m→92, 1y→365, "12"→365, invalidi→3 mesi).

- **Backoffice — durata sessione (access token) portata ad almeno 3 ore** — Simone: "il token del backoffice scade troppo rapidamente, deve durare almeno 3 ore". In `backend/src/auth/auth.module.ts` il default di `JWT_ACCESS_TTL` passa da **`15m` a `3h`** (la env su Render, se impostata, ha comunque la precedenza). Il refresh token resta invariato.


- **Login master password — non scattava: minimo abbassato a 8 + confronto tollerante** — Simone segnalava che la `MASTER_PASSWORD` di servizio non permetteva l'accesso **né in backoffice né in app**. Due cause: (1) il guard `masterPassword.length >= 16` **ignorava in silenzio** una master password più corta (la sua lo è); (2) un eventuale **spazio/a-capo finale** incollato nella variabile su Render faceva fallire il confronto senza segnalazione. **Fix in `backend/src/auth/auth.service.ts`:** minimo estratto in costante **`MASTER_PASSWORD_MIN_LENGTH = 8`** (era 16) e **`.trim()`** applicato sia al valore della env (`MASTER_PASSWORD`) sia alla password digitata, prima del confronto a tempo costante. Nessun'altra modifica alla logica (bypassa argon2 solo se combacia; audit `auth.master_login` invariato). **Requisiti per l'uso:** il valore su Render deve restare **≥8 caratteri** e l'account in cui si entra deve **già esistere** (il master password non crea utenti). ⚠️ **Sicurezza:** 8 caratteri aprono *qualsiasi* account → consigliato usarne uno lungo (≥16), tenuto solo fra le env di Render. Backend non verificabile in sandbox (Prisma engine 403), ma è una modifica banale (costante + trim), nessun rischio di tipo.

- **CRM + Marketing — classificazione persona (Cliente / Storico / Lead)** — richiesta di Simone ("mettila e fai in modo che possa utilizzarla anche per il marketing"); completa il lavoro sull'upload dei clienti/lead storici. **CRM (`LeadsTable.tsx`):** il vecchio badge generico "lead" è ora un **badge a 3 stati** — **Cliente** (verde, `stage==='paid'`), **Storico** (viola, `historicalPaidCents>0` e non paid = pagamenti pre-Metabole), **Lead** (ambra, nessun pagamento) — con tooltip esplicativo; `historicalPaidCents` aggiunto all'interfaccia (già restituito dal backend via `include`). **Marketing (`Marketing.tsx` + `marketing.service.ts`):** la tendina "Tipo" diventa **"Tipo persona"** con Tutti / Cliente / Storico / Lead, collegata a un nuovo filtro **`segment`** nel service (`client` → `stage:'paid'`; `historical` → `historicalPaidCents>0` & `stage≠paid`; `lead` → `stage≠paid` & `historicalPaidCents` nullo/≤0), coerente col badge CRM. Il DTO passa i filtri come oggetto libero → nessuna modifica. Allineato con `publicStats` del sito (clienti seguiti = paganti ∪ storici). Type-check backoffice **0 errori** (contro `origin/main` aggiornato); backend con soli campi Prisma validi. **Deployato** (commit `62cca74`, che include anche il fix `publicStats`/Impostazioni che sblocca la build Render).

## 2026-07-14


- **Motore E5 (R12) — routing delle segnalazioni per categoria** — le escalation ora hanno una **categoria** standard per essere instradate al ruolo giusto. Campo `Escalation.category` (nullable, migrazione additiva `20260714160000_escalation_category`, validata PG16 + idempotente): `diet_blocked | no_progress | low_adherence | mood_risk | clinical | other`. **Costanti di routing** (`escalation-routing.ts`): `diet_blocked`/`no_progress` → nutrizionista (+ coach informato), `low_adherence`/`mood_risk` → coach, `clinical` → solo nutrizionista. Nuovo **`EscalationRoutingService.open()`**: apre una segnalazione categorizzata assegnandola al membro del team **già assegnato** alla cliente per quel ruolo (se assente resta al pool), **idempotente** per (cliente, categoria). Il modulo escalations ora esporta il service; l'elenco `GET /admin/escalations` accetta `?category=`. **Retrofit sicuro dei creatori esistenti** (categoria impostata, assegnatario invariato): `personal-base` e `menu` → `diet_blocked`, screening onboarding → `clinical`. Build ok, **test 35/35** (5 nuovi sul routing). **Rimane come rifinitura del socio (sua area scoring):** la **modulazione dei pesi da `objective`** (mantenimento = efficacia neutra, niente deficit) — piccola e va decisa con lui; e l'adozione del routing per le sue segnalazioni coach/segnali (`low_adherence`/`mood_risk`/`no_progress`), per cui il service è già pronto. **Con questo E1–E5 (R8–R12) sono coperti** salvo quelle due rifiniture.

- **Motore E4 (R11) — penalità di ripetizione (completa la formula di scoring)** — analizzato lo stato: il socio ha **già implementato** gran parte di R11 in `MenuService` (scoring `w_eff·efficacia(MenuWeight) + w_grad·gradimento(stelle)` con **modulazione per stato**: conforto→gradimento, plateau/post-evento/rientro→efficacia, pre-evento→proteine) e l'**apprendimento per-pasto** in `DietLearningService` (aggiorna `MenuWeight`, con `distinctiveCredits` che "isola il pasto" dando più credito alla ricetta rara/cambiata). Anche l'**exploit/explore** del metodo è di fatto realizzato dagli **stati dell'agente**: `plateau/rientro` spinge sull'efficacia → sceglie le ricette che hanno fatto perdere di più (exploit), `normale` bilancia (explore). Mancava **un solo termine** della formula R11: la **penalità di ripetizione** (`− penalità(ripetizione)`). Aggiunta a `buildScoringContext`: conta quante volte ogni ricetta è stata servita nella finestra `menu_repeat_window_days` (default 14) e sottrae `menu_penalty_repeat · ripetizioni` dal punteggio → favorisce la **varietà**. **Config-gated: default 0 = disattivata**, quindi il comportamento attuale del socio **non cambia** finché non la si alza (valori tipici 0,05–0,2 dai `/parametri`). Additivo, nessuna modifica allo schema. Build ok, **suite menu 19/19** (2 nuovi test: con penalità evita la ricetta recente, senza penalità comportamento invariato). **NB per il socio:** ho toccato `menu.service.ts` solo per aggiungere il termine di penalità (gated); la logica di selezione/apprendimento resta la tua. **Con questo R8–R11 (E1–E4) sono coperti;** resta **E5** (stati contestuali già presenti → mancano gli input/soglie fini + **routing escalation per categoria** R12).

- **Contabilità — selettore mese + export report PDF/CSV** — su richiesta di Simone. Il periodo non è più un range Dal/Al ma un **selettore del mese** (`<input type="month">`, default mese corrente, max = mese corrente): scelto il mese, la pagina si aggiorna da sola su primo↔ultimo giorno. Aggiunti due pulsanti **Report PDF** e **Report CSV** che scaricano il conto economico del mese: riepilogo (incassi/costi/utile/margine), KPI (nuovi clienti, paganti, marketing, CAC, ARPU), costi per categoria e serie mensile. **Backend:** `AccountingService.reportPdf` (pdfkit, riusa l'infra già presente in `ReportsService`) e `reportCsv` (separatore `;`, decimale virgola, BOM → Excel IT); endpoint `GET /admin/accounting/report/pdf` e `/report/csv` (@Roles admin, ritornano `{fileName, mimeType, contentBase64}`). **Frontend:** download via base64→blob. Build backend ok, type-check backoffice **0 errori**. Nessuna modifica allo schema.

- **Motore E3 (R10) — ciclo bigiornaliero attivo + gradimento** — nuovo `CycleModule` (additivo). Il socio già **eroga** il menu 2 giorni alla volta (`MenuService`→`MenuDay`) e **chiude** il ciclo al 2° giorno (`DietLearningService`→`CycleFeedback` + learning `MenuWeight`); mancava il pezzo **"cosa mangia ORA"**. `CycleService.getActiveCycle` materializza il **ciclo attivo** (`ClientCycle`) dalle ultime N giornate erogate (`menu_days_delivered`, default 2): finestra del ciclo, le **2 cotture** (R5/R6 — stessa kcal, preparazioni diverse: prende i tipi da `Recipe.cookingMethods`), lo **stato contestuale** (ancora per R11, conservato tra le riletture, default `normale`) e l'**esito dell'ultimo ciclo** chiuso. **Regola gradimento R10:** il menu vale il **MAX** delle stelle delle sue ricette (default **5★** se non valutate, in `config_param cycle_default_rating`); il gradimento del ciclo è il **MIN** tra i menu (il "pasto peggiore" traina, per non ripetere ciò che non piace). Endpoint `GET /me/cycle` (cliente) e `GET /clients/:id/cycle` (coach/nutrizionista). **Additivo:** legge i `MenuDay`, non tocca erogazione né chiusura; nessuna modifica allo schema (`ClientCycle` già scaffoldato), **nessuna migrazione**. Build ok, **test 7/7**. **Prossimo motore:** E4 (R11 — scoring exploit/explore + apprendimento per-pasto) e E5 (stati contestuali + routing escalation R12).

- **Motore E2 (R9) — partenza differenziata + unicità certificata** — prossimo step del piano motore dopo E1. Integrato in `PersonalBaseService.buildPersonalBase`: **(a) seme deterministico** derivato dal `client_id` (HMAC-SHA256 con chiave server `ENGINE_SIGNING_KEY`, fallback `FILE_ENCRYPTION_KEY`) → la base viene **ordinata in modo unico e riproducibile** per ogni cliente (due clienti con la stessa dieta e stesse ricette sicure ricevono lo **stesso insieme** ma in **ordine diverso** = partenza differenziata); **(b) collision check**: si calcola la firma `HMAC(seme, versione, ricette ordinate)` e, se coincide con quella di un'altra cliente, si **perturba il seme e si riordina** (fino a 5 tentativi) per garantire unicità; **(c) certificato firmato** salvato in `PersonalizationCertificate` (seme, firma, versione) — verificabile. Nuovo endpoint `GET /clients/:id/personalization-certificate` (nutrizionista/admin) che **ricalcola e valida** la firma (`valid: true/false`). Lo stato `GET /me/personal-base` ora include `certificate {version, signature}`. Nessuna modifica allo schema (`PersonalizationCertificate` era già scaffoldato); nessuna migrazione. Build ok, **test personal-base 11/11** (4 nuovi: firma, determinismo, unicità, collisione). **Scelta di rigore (decisa da noi, dominio motore del socio):** seme+HMAC verificabile lato server, non firma asimmetrica/PKI (sufficiente per l'auditor interno; upgradabile). **NB:** volendo si imposta `ENGINE_SIGNING_KEY` su Render (altrimenti usa `FILE_ENCRYPTION_KEY`); le firme si rigenerano a ogni rebuild, quindi cambiarla non rompe nulla. **Prossimo motore:** E3 (R10 — ciclo bigiornaliero + monitoraggio).

- **Pagamenti — gestione contabile completa (backoffice + app cliente + auto-annullo)** — su richiesta di Simone. **Backoffice (Bonifici & contabilità):** in ogni riga in attesa l'operatore può ora **vedere la contabile**, **Approvare**, **Rifiutare** ed **Eliminare** (con conferma). Approva/Rifiuta ora funzionano **anche nel tab "In attesa contabile"** (pending), non solo dopo il caricamento — l'operatore può aver già visto il bonifico in banca. **"Elimina" = annulla e resta nello storico** (nuovo stato `cancelled`, nuovo tab **Annullati**), non cancella nulla. **App cliente (Profilo → storico):** per un pagamento in attesa la cliente può **caricare la contabile** (PDF/immagine → base64, endpoint già esistente) o **annullare l'ordine** (`POST /me/payments/:id/cancel`). **Auto-annullo:** il cron giornaliero annulla i bonifici rimasti *pending* (contabile mai caricata) oltre **`payment_pending_auto_cancel_days`** (default **10**, in `config_param`); i pagamenti con contabile già caricata NON si toccano (aspettano l'operatore). **Backend:** stato `cancelled` (migrazione `20260714150000_payment_cancelled`, validata PG16 + idempotente), `cancelPayment` (operatore/cliente/cron) che chiude anche abbonamento/ordine in sospeso, `autoCancelStalePayments` agganciato a `/internal/cron/daily`, guardie di approve/reject estese a pending. Endpoint: `POST /admin/payments/:id/cancel`, `POST /me/payments/:id/cancel`. Build backend ok, **test commerce 14/14** (aggiornato il test del vecchio comportamento "senza contabile non si approva" → ora si può), type-check backoffice + app **0 errori**.

- **Backoffice — casella di posta configurabile dalle Impostazioni + messaggio se non collegata** — su richiesta di Simone (la casella admin non si apriva). La casella `@metabole.eu` ora si collega dalle **Impostazioni** (nuova card "Casella di posta": stato collegata/scollegata, collega con email+password → `PUT /me/mailbox`, **Scollega** con conferma → `DELETE /me/mailbox`), accessibile a **tutti gli staff, admin incluso** (il backend `/me/mailbox` abilita già `admin`). La pagina **Posta**, se la casella non è collegata, non mostra più il form inline ma un messaggio chiaro con pulsante **"Vai in Impostazioni e collega la casella"** (link a `/impostazioni`). Rimosso il componente `MailboxSetup` ora inutilizzato. Type-check backoffice **0 errori**; `Posta.tsx`/`Impostazioni.tsx` allineati al device prima della consegna. Nessuna modifica backend (gli endpoint casella esistevano già). **Prossimo:** pagamenti (azioni contabile in backoffice + carica/annulla lato cliente + auto-annullo a 10 giorni).

- **Motore E1b — agente esclusioni (base personalizzata sicura)** — costruito il cuore di E1: `PersonalBaseService.buildPersonalBase(clientId)` prende il pool di ricette del prodotto scelto dalla cliente e lo filtra in **sicurezza** sui suoi **allergeni codificati** (14 UE), tenendo solo ricette **attive + confermate dal nutrizionista + regime-compatibili + senza allergeni della cliente**. Se per un pasto principale (colazione/pranzo/cena) restano meno di **`personal_base_min_recipes_per_slot`** (default 3, in `config_param`) ricette sicure — **oppure** la cliente ha un'allergia **fuori dai 14 codici** (testo libero, non abbinabile ai tag) — la base **non è certificabile in automatico**: si apre una segnalazione **"Piano bloccato"** (`source: engine`) assegnata al nutrizionista e la app mostra il messaggio d'attesa del socio ("Stiamo perfezionando il tuo menu…"). Se invece è tutto ok, salva la base in **`ClientMenuPool`** (nuova versione) e **chiude** le segnalazioni aperte. **Trigger:** fine onboarding, aggiornamento profilo (regime/stile/pasti), e pulsante **"rigenera base"**. Endpoint: `GET /me/personal-base` (stato), `POST /me/personal-base/rebuild` (cliente), `POST /clients/:id/personal-base/rebuild` (nutrizionista). Chiamate ai trigger **non bloccanti** (l'onboarding non fallisce mai per la base). Parametro motore aggiunto al seed. Build ok, **test 7/7 verdi**. Nessuna modifica allo schema (i modelli `ClientMenuPool` ecc. erano già scaffoldati). **Manca (UI):** il pulsante "rigenera base" e la card di stato nella app cliente.

- **Motore E1a — allergie strutturate in onboarding (prerequisito di E1b)** — la domanda allergie dell'onboarding non è più testo libero: ora è una **checklist sui 14 allergeni UE** (gli stessi codici taggati sulle ricette), così l'agente esclusioni può filtrare in sicurezza. Aggiunto un campo separato **"Altra allergia non in elenco"** (testo libero) che confluisce nelle allergie ma **forza la revisione del nutrizionista** (E1b la tratta come non-codificata → blocco prudente). Modifiche: `onboarding.questions.ts` (campo `multi_choice` + `allergiesOther`), DTO (`allergiesOther?: string[]`), `onboarding.service` (merge nelle allergie salvate, entrambi i rami create/update), app `Onboarding.tsx` (mapping nel submit). Build backend + type-check app **0 errori**.

- **Backoffice — pagina "Allergeni ricette" (UI di E1a)** — completa il taggaggio allergeni: nuova pagina (sezione Catalogo, rotta `/tag-allergeni`) dove il nutrizionista vede le ricette col loro **stato** (Confermata / Da rivedere) e conferma gli allergeni. Modale con i **14 allergeni UE** in checklist, i suggeriti dal pre-tag evidenziati con ✨ (e tooltip "rilevato da: …"), pre-selezione = attuali ∪ suggeriti; "Conferma" → `PATCH /recipes/:id/allergens` (segna reviewed). Filtro "solo da rivedere" di default. Usa gli endpoint E1a già deployati. Type-check 0 errori. Con questa, il gate di attivazione prodotto è operativo end-to-end (tagga le ricette → attiva il prodotto). **Restano di E1:** E1b (agente esclusioni) e allergie strutturate in onboarding.

- **Login backoffice — occhiolino "mostra password" (press-and-hold)** — accanto al campo password del login, pulsante occhio: **tieni premuto → mostra** la password, **rilascia → nasconde** (mousedown/up/leave + touch). Icona `ti-eye`/`ti-eye-off`, `tabIndex=-1`. Modifica in loco sul device (`Login.tsx` del socio, sandbox indietro). *Nota separata:* segnalato a Simone che, col blocco della password admin, il recupero admin passa SOLO da Render (`ADMIN_PASSWORD` + `ADMIN_PASSWORD_RESET=true` → deploy → rimuovere il flag) — "Password dimenticata" non vale più per l'admin (voluto).

- **Casella mail — interfaccia backoffice (pagina Posta)** — UI della casella @metabole.eu (MVP, staff): nuova pagina **Posta** (`Posta.tsx`) con collegamento casella (email + password, salvata cifrata lato server), **posta in arrivo** (lista + apri messaggio, segna letto), **scrivi/invia** e **rispondi**. Precompila la composizione da `?to=&subject=&body=` (pronta per il pulsante "Scrivi al cliente" da appuntamento/scheda). Rotta `/posta` in `App.tsx`, voce menu **Posta** in `Layout.tsx` (sempre visibile). Type-check 0 errori. Consegna complicata dalla disconnessione del desktop + restrizione unlink del git nella VM (git VM inaffidabile): file scritti fisicamente sul disco, verificati a mano; GitHub Desktop sul Mac li rileva. **Restano:** pulsante "Scrivi al cliente" da appuntamento/scheda; rendering HTML sanitizzato (per ora solo testo).

- **Sicurezza — password admin gestita SOLO da `ADMIN_PASSWORD` (Render)** — su richiesta di Simone: la password dell'account **admin** non è più modificabile dall'app. Aggiunto un guard in **`users.service.changePassword`** (auto-cambio `/me/password`) e in **`auth.service.confirmPasswordReset`** (reset via email "password dimenticata"): se l'utente è `role: 'admin'` → rifiuto con messaggio "si gestisce solo dalla variabile ADMIN_PASSWORD su Render". Gli **altri utenti** cambiano password normalmente. Nessun lock-out: l'admin è creato dal seed senza `mustChangePassword`. **NB (per il socio):** ho toccato due file della tua area auth (`users.service.ts`, `auth.service.ts`) con due guard minimali — modifica in loco sul device (il mio sandbox era indietro sul tuo merge `mustChangePassword`).

- **Motore E1a — taggaggio allergeni ricette (R8, sicurezza)** — prima parte di E1 (agente esclusioni), su via libera del socio (`Metabole_E1_Agente_Esclusioni_Decisioni.md`). **Ricetta:** campi `allergens String[]` (14 allergeni UE, codici) + `allergensReviewed Boolean` (una ricetta NON confermata dal nutrizionista non è considerata sicura) + migrazione `20260714140000_recipe_allergens` (validata PG16). **Pre-tag assistito** (`allergens.ts`): dizionario keyword→14 allergeni UE (con derivati: `latte` copre burro/formaggi/panna) che **suggerisce** i tag dagli ingredienti; il nutrizionista **conferma** (mai auto-tag silenzioso). Endpoint ricette (nutrizionista): `GET /recipes/:id/allergen-suggestions`, `PATCH /recipes/:id/allergens`. **Gate di attivazione R8:** un prodotto diventa `clientVisible` solo se **tutte** le ricette dei suoi menu sono `reviewed` **e** c'è almeno un gruppo di equivalenza approvato (`assertActivatable` in `updateDietProduct`). Test dizionario 7/7 + catalogo 8/8, build ok. Sandbox riallineato al `mustChangePassword` del socio prima di toccare lo schema (checksum verificati). **Prossimo E1a:** UI backoffice per taggare le ricette. Poi E1b (agente esclusioni) e allergie strutturate in onboarding.

- **Casella mail @metabole.eu — backend MVP (webmail, solo staff)** — integrazione della posta del nostro hosting. **Modello `MailAccount`** (userId unico, email, **password casella cifrata a riposo** AES-256-GCM riusando `crypto.util` + `FILE_ENCRYPTION_KEY`) + migrazione `20260714130000_mail_account` (validata PG16). **`MailboxService`**: `setAccount` (prova la connessione IMAP poi salva cifrato), `listInbox` (IMAP via imapflow), `getMessage` (segna letto, parse via mailparser), `send` (SMTP via nodemailer). Server **mail.metabole.eu** IMAP 993 / SMTP 465 SSL, uguali per tutti → in env con default. Endpoint `/me/mailbox` (`@Roles` staff): status, PUT (imposta), DELETE, inbox, message/:uid, send. **Password mai loggata né restituita.** Dipendenze aggiunte (imapflow/nodemailer/mailparser + tipi) con **lockfile rigenerato** e `npm ci` verificato. Test cifratura 4/4, build ok. **Attenzione:** le password casella dipendono da `FILE_ENCRYPTION_KEY` (non va rigenerata, o le password salvate diventano illeggibili). **Prossimo:** UI backoffice (pagina Posta: inbox + lettura + composizione) e pulsante "Scrivi al cliente" da appuntamento/scheda cliente.

- **Marketing — Publisher: import collezioni dal catalogo** — le **8 collezioni "pronta"** del socio (`catalogo_vignette.json`: maria_matrimonio, menopausa, post_gravidanza, rientro_vacanze, giornata_storta, persone_vere_ai, su_misura, schermate_reali) si importano nella coda con un clic. Snapshot del catalogo incorporato come modulo TS (`vignette-catalog.data.ts`, sempre disponibile a runtime, niente file/asset da copiare), endpoint `POST /admin/social-posts/import` idempotente (salta le collezioni già importate, match per `collectionId`), pulsante **"Importa dal catalogo"** nella pagina Publisher. Ogni collezione → bozza con caption/hashtag/canale/immagine (design_id Canva o PNG) reali → poi passa dal Giudice come le altre. Build ok, type-check 0 errori, checksum verificati. **NB:** è una fotografia del catalogo; se il socio lo aggiorna va riallineato il modulo.

- **Marketing — pagina backoffice Publisher (coda post)** — nuova pagina **Publisher social** (sezione Marketing) per il flusso completo: crea post (canale, caption, hashtag, collezione/immagine), **Giudica** (mostra ✓ Conforme o i problemi in rosso), **Approva** (solo se conforme), **Segna pubblicato** (con id piattaforma opzionale), Modifica (riporta in bozza → rigiudica), Elimina. Rotta `/publisher` + voce menu (`pageKey: marketing`). Type-check backoffice 0 errori, checksum verificati. Completa la fondazione Publisher lato UI. **Restano:** import collezioni dal `catalogo_vignette.json`, e (con credenziali) l'auto-publish + export Canva.

- **Marketing — agente Publisher (fondazione backend)** — primo pezzo del Publisher dal catalogo vignette del socio. **Modello `SocialPost`** (coda/log: collezione, canale, caption, hashtag, riferimento immagine canva/png, stato, esito Giudice, schedulazione, pubblicazione) + migrazione `20260714120000_social_post` (validata PG16). **Giudice** (`GiudiceService`) = gate di compliance con le regole prese dal catalogo del socio (no prima/dopo, no numeri/misure/tempi/garanzie, no seconda persona su attributi fisici) — **6/6 test verdi**, euristica prudente + niente falsi positivi. **`PublisherService`**: macchina a stati draft→judged→approved→(scheduled)→published, si **approva solo se il Giudice passa**. Endpoint `/admin/social-posts` (`@Roles marketing/head_marketing/admin`): create/update/judge/approve/schedule/publish/reject/delete. **Nota:** l'auto-publish su Instagram/Facebook e l'export PNG da Canva richiedono credenziali non ancora presenti → per ora la pubblicazione si **registra a mano** (`publish` = markPublished con id piattaforma). Build ok. **Prossimo:** pagina backoffice (coda Publisher) + eventuale import delle collezioni dal catalogo.

- **Backoffice — gestione gruppi di equivalenza (R4/R8)** — il nutrizionista può ora rivedere/approvare i 23 gruppi caricati come bozza. **Backend**: nuovo `EquivalenceModule` — `GET/POST/PATCH /equivalence-groups`, `POST /:id/approve|unapprove`, `DELETE /:id` (`@Roles nutritionist/head_nutritionist/admin`); service con workflow draft→approved + bump versione, `members` gestito come `{ items, note }`. **Backoffice**: nuova pagina **Gruppi di equivalenza** (sezione Catalogo) — tabella (nome, alimenti, ambito globale/prodotto, stato), modale crea/modifica (alimenti uno-per-riga + nota di sicurezza), approva/rimetti-in-bozza, elimina. Build backend ok, type-check backoffice 0 errori; checksum dei file editati verificati vs device. **Nota:** il motore (E1) userà SOLO i gruppi `approved`. Additivo, nessuna logica motore.

- **Motore — seed dei 23 gruppi di equivalenza (R4/R8)** — caricati come base i **23 gruppi** validati dal socio (`percorsi/keto/regola4_sostituzioni.md`) in `EquivalenceGroup`: `seedEquivalenceGroups()` idempotente, gruppi **globali** (`productId: null`), **`status: draft`** (il nutrizionista li rivede/approva dal backoffice). `members` salvato come `{ items, note? }` per conservare le note di sicurezza (allergeni/etichette). 23 gruppi, nomi unici verificati; build ok. È il dato di partenza per l'agente esclusioni (E1). Additivo, nessuna logica motore.

- **Motore — E0 avviato: allergie + scheletro modelli (via libera del socio)** — applicate le decisioni di `Metabole_Motore_R8_R12_Decisioni.md`. **(1) Allergie (D5):** campo `ClientProfile.allergies String[]` (blocco duro, distinto da intolleranze/gusti) + **domanda onboarding dedicata** (input allergie separato nello schermo "Allergie e intolleranze") + salvataggio in `submitAnswers` + mapping nel submit dell'app. **(2) Scheletro modelli (senza logica):** `EquivalenceGroup` (gruppi di equivalenza R4/R8, del nutrizionista, draft→approved, `productId?` = globale), `ClientMenuPool` (base personalizzata R8, isolata per `client_id`), `ClientCycle` (ciclo bigiornaliero R10 + **stato contestuale** R11: normale/conforto/rientro/pre-post-evento/plateau), `PersonalizationCertificate` (unicità R9: seme+firma HMAC). **Migrazione** `20260714110000_engine_r8r12_skeleton` (ALTER client_profile + 4 tabelle) **validata su PG16**. Tutto **additivo**, non tocca il motore giornaliero. Build backend `dist/main.js` ok, type-check app **0 errori**. Checksum di tutti i file editati verificati identici al device prima della consegna (dopo la lezione sullo schema). **Prossimo:** E1 (R8 — agente esclusioni), quando concordato.

- **Sito — collegati gli endpoint dinamici alla produzione** (task checklist go-live "[Sv] Endpoint dinamici del sito", riga 49) — riempiti sul `<body>` di `Metabole_Sito_Presentazione.html` i tre attributi vuoti con l'URL di produzione: `data-paths-endpoint`, `data-stats-endpoint`, `data-testimonials-endpoint` → `https://metabole-backend.onrender.com/api/v1/public/...` (stessa base già usata dal form lead). `data-i18n-endpoint` lasciato vuoto (endpoint non previsto → il sito fa no-op). `Metabole_Lavora.html` già collegato ai lead. **Perché funzioni online serve:** (1) il **dominio del sito** (es. `https://metabole.eu` / `https://www.metabole.eu`) in **`CORS_ORIGINS`** su Render — la checklist elenca solo app+backoffice, quindi va aggiunto il sito; (2) **deploy del sito** sotto il suo dominio (statico, es. Vercel), non ancora predisposto. Fatto questo, home (percorsi + numeri) e sezione storie si popolano da soli, zero-redeploy.

- **Motore — piano di sviluppo R8–R12 mappato sullo schema reale** (`progetto/Metabole_Motore_Piano_R8_R12.md`) — tradotte le 12 regole del *Metodo del Motore Intelligente* del socio in un piano concreto sul nostro schema. **Emerso:** il socio ha già scaffoldato i modelli di R10/R11 (`CycleFeedback` = peso/cm separati + seguito sì/no, `RecipeRating` = gradimento, `MenuWeight` = ranking personale per ricetta); Fase A (R1–R7) coperta da `Diet`/`DietDayTemplate`/`Recipe`+approvazione. **Mancano** i pezzi R8–R9 (base personale per cliente, gruppi di equivalenza, agente esclusioni, seme/collision/certificato di unicità) + un campo **`allergies`** dedicato su `ClientProfile` (oggi solo intolleranze/gusti) + modello "ciclo attivo" (R10) + logica scoring/stati (R11) + routing escalation per categoria (R12). Proposto piano a **fasi additive E1–E5** (parametrizzate per `product_id` via `ProductRule`) e **6 domande aperte per il socio** (confini motore-giornaliero vs agente, gruppi equivalenza, rigore unicità, stati contestuali, onboarding allergie). Stato: **proposta da allineare col socio, nessun codice motore scritto.**

- **⚠️ Fix regressione schema Prisma (mia, causata da copia stale) → build Render ripristinato** — nel consegnare le testimonianze, lo `schema.prisma` preso via *staging* dal Mac era una **copia vecchia** (sync iCloud in ritardo sui file grossi): consegnandola ho **cancellato ~242 righe** dello schema del team (ruoli `marketing`/`head_marketing`, `secondaryEmail` su User, `ProductRule`, `RuleProposal`, `CycleFeedback`, `MenuWeight`). Render, rigenerando il client, falliva su `users.service.ts` (campi/enum mancanti). **Fix:** schema ricostruito dal commit buono in git (`97dcb9e`) + ri-aggiunto solo il modello `Testimonial`; scritto direttamente sul file vero (device_bash, non staging). Verificato: 1426 righe, `Role` con marketing/head_marketing, `secondaryEmail` presente, tutti i modelli, giunzione pulita, graffe bilanciate. Gli altri file del commit (seed/app.module/backoffice/testimonials) erano solo-aggiunte, nessun revert. **Lezione di processo:** per i file grossi NON fidarsi della copia *staged* (può essere stale) — leggere/scrivere il file vero con device_bash e confrontare con `git`. La procedura mail-secondaria↔primaria (user + clienti) è integra: dipende da `secondaryEmail`, ora ripristinato.

- **Sito — testimonianze dinamiche (`GET /api/v1/public/testimonials`) + gestione backoffice** — terzo e ultimo endpoint dinamico del sito (`data-testimonials-endpoint`): ritorna `[{ name, age?, text, photo? }]` delle sole testimonianze **pubblicate**, ordinate. **Nuovo modello `Testimonial`** (name, age?, text, photo?, locale, published, order, source) + **migrazione** `20260714100000_testimonial` (validata su PG16). Nuovo `TestimonialsModule`: endpoint pubblico `@Public` + admin CRUD `@Roles('admin')` (`GET/POST/PATCH/DELETE /admin/testimonials`). **Seed idempotente**: 3 testimonianze iniziali (le stesse del prototipo: Martina/Elena/Giulia) se la tabella è vuota. **Backoffice**: nuova pagina **Testimonianze** (sezione Marketing) — tabella + modale crea/modifica, pubblica/nascondi, elimina, ordine. Test service **3/3 verde**, build backend `dist/main.js` ok, type-check backoffice **0 errori**. → **Con questo il sito è completo lato dati dinamici**: percorsi + numeri + testimonianze, tutti gestibili senza redeploy.

- **Sito — endpoint numeri home (`GET /api/v1/public/stats`)** — secondo endpoint dinamico del sito (`data-stats-endpoint`): ritorna `{ clients, reached, methods, years? }`. Ogni campo legge da **`config_param`** (`site_stats_clients`, `site_stats_reached`, `site_stats_years`) con **fallback ai conteggi reali del DB** (`clients`=n° schede cliente, `reached`=n° lead CRM, `methods`=n° percorsi visibili per stile, coerente con `/public/paths`; `years` mostrato solo se configurato). **Così la decisione "numeri reali vs base marketing 20.000/80.000 diventa un toggle dal backoffice, senza redeploy** — non serve scegliere adesso lato codice. `@Public` + `@Throttle` 60/min. Test `publicStats` aggiunto (dedup per stile + fallback). Suite catalogo **8/8 verde**, build completo `dist/main.js` ok. → **Per attivare i numeri marketing** (quando vuoi): impostare dal backoffice/admin-config i parametri `site_stats_clients=20000`, `site_stats_reached=80000`, `site_stats_years=<anni>`. **Resta del sito:** `/public/testimonials` (serve modello Testimonial).

- **Deploy backend LIVE + verifiche** — dopo i tre fix, Render è **verde** (commit merge `832284d`): build ok, preDeploy `migrate deploy` + seed (incluso il **seed Keto** del socio) ok, avvio `dist/main.js` ok. **Verificato in produzione:** `GET /api/v1/public/paths` risponde con i 4 percorsi (Mediterranea/Proteica/Keto/Low-carb) nel formato atteso dal sito — il Keto compare, quindi il suo seed ha popolato il DB. **Handoff socio (`Metabole_Handoff_Simone.pdf`) chiuso lato codice:** 4 file Keto revisionati, seed già girato nel preDeploy, **test `seed_keto.spec.ts` 3/3 verde** (integrità dati 118 ricette + creazione + idempotenza). Merge della pull pulito, nessun conflitto, fix Render intatto. **Restano solo (Simone/Ops):** config prod sui pannelli (Stripe LIVE+webhook, Neon prod, Brevo SPF/DKIM, `FIREBASE_SERVICE_ACCOUNT`, ADMIN/CORS/`VITE_API_URL`) e attivazione push sul client (`cap sync android` + rebuild APK).

- **Fix avvio Render (`Cannot find module dist/main.js`)** — il build passava (nest build + seed + pre-deploy ok) ma l'avvio falliva. **Causa reale:** il file `.tsbuildinfo` dell'incrementale TS viene scritto in `backend/tsconfig.build.tsbuildinfo` (radice), **fuori** da `dist/`; `deleteOutDir` cancella `dist/` ma non il tsbuildinfo, e Render **ripristina la cache** → `tsc` incrementale crede "niente cambiato" e **non ri-emette** `main.js`. **Fix in `tsconfig.build.json`:** `incremental: false` (con `deleteOutDir` non serve) + `rootDir: src` e `include: [src/**/*]` per bloccare l'output in `dist/main.js` a prescindere (evita anche il rischio `dist/src/main.js` se `seed.ts` entrasse nella compilazione). Aggiunto `*.tsbuildinfo` al `.gitignore`. Riprodotto e verificato in sandbox: con tsbuildinfo stale la build ora emette **sempre** `dist/main.js`. Tipizzato anche `r` in `push.service.ts` (noImplicitAny). **Lezione:** con `deleteOutDir: true` + cache CI, l'incrementale TS va disattivato.

- **Fix build Render (due blocchi in fila)** — il deploy backend falliva: (1) **`npm ci`** si fermava con decine di "Missing … from lock file" perché `firebase-admin` (aggiunto per le push) era in `package.json` ma il **`package-lock.json` non era stato rigenerato** → lock rigenerato e allineato (verificato `npm ci` pulito, 163 pacchetti firebase); (2) subito dopo **`nest build`** dava `TS2304: Cannot find name 'UpdateDietProductDto'` → mancava l'import in `catalog.service.ts`, aggiunto. Type-check completo passato (i soli residui sono artefatti del client Prisma non rigenerabile in sandbox, non presenti su Render). **Lezione:** quando si aggiunge una dipendenza al backend, rigenerare **sempre** `package-lock.json` (Render usa `npm ci`, che pretende i due file allineati).

- **Sito — endpoint pubblico percorsi (`GET /api/v1/public/paths`)** — primo endpoint dinamico per il sito di presentazione (`data-paths-endpoint`): ritorna le diete `clientVisible=true`, **una per stile**, nel formato che il sito si aspetta (`name` con fallback su `clientName`, più `description`/`highlights`/`objective`/`seasonalTag`). `@Public` + `@Throttle` 60/min, nuovo `PublicCatalogController`, `CatalogService.publicPaths()`. **Nessuna migrazione, zero-redeploy**: aggiungere/modificare un percorso dal backoffice si riflette sul sito. → **Azione tua/Ops:** come per i lead, il dominio del sito deve stare in `CORS_ORIGINS` su Render. **Restano (da decidere col socio):** `GET /public/stats` (numeri home: conteggi reali DB vs base marketing 20.000/80.000), `/i18n/site`, blog, testimonial.

- **Go-live blocker #2 risolto — scoping decisioni motore per-paziente** — spostato il controllo di assegnazione **dentro `engine.reviewDecision`**: un nutrizionista può confermare/correggere SOLO le decisioni dei propri pazienti (capo/admin qualsiasi). Ora è protetta **ogni** via, anche l'endpoint diretto `/engine/decisions/:id/confirm|correct` (prima solo `/nutritionist/...` era scoped). Aggiunto test dedicato. **Test verdi: 10/10 motore + 11/11 nutrizionista.**

- **Go-live blocker #1 risolto — endpoint pubblico lead** — applicato l'handoff del socio (`Metabole_Lead_Endpoint_Handoff.md`): nuovo `POST /api/v1/public/leads` (`@Public` + `@Throttle` 5/min + **honeypot** `website`), `CrmService.createPublic()` (riusa `CrmRecord`, metadati in `stageDates.lead_in.meta`, dedup soft per email — **nessuna migrazione**), `PublicLeadDto`, registrato in `CommerceModule`. I form del sito (contatti + Lavora) **non perdono più i lead**. → **Azione tua/Ops:** aggiungere il dominio del sito a `CORS_ORIGINS` su Render (es. `https://www.metabole.eu,https://metabole.eu`). ✅ *CORS_ORIGINS aggiornata su Render.*

## 2026-07-13


- **Prodotti dinamici — Fase F foundation (regole per prodotto)** — dal catalogo del socio: le regole opzionali (L5, L7, stati agente A1–A6, ecc.) sono **mappate su `config_param`**, quindi si agganciano al nostro motore. Aggiunti i modelli **`ProductRule`** (dietId, ruleCode, enabled, params) e **`RuleProposal`** (coda "c'è un'altra regola?") + migrazione validata su PG locale. Endpoint: `GET/PATCH /diets/:id/rules` (upsert regole attive/parametri) e `POST /diets/:id/rule-proposals`. **Additivo, non tocca il motore.** **Restano:** (F-UI) lo step "regole" nel wizard backoffice (checklist regole opzionali con consenso + parametri); (E) **agente/motore che legge le ProductRule per prodotto** e sovrascrive i default globali config_param — parte delicata, da allineare col socio (è il suo motore).

- **Prodotti dinamici — Fase D completata (modifica scheda cliente)** — ora la scheda cliente di un prodotto si può **modificare** su una dieta esistente (prima solo alla creazione), **anche se approvata** (non tocca i menu): nuovo endpoint `PATCH /diets/:id/product` (aggiorna solo i campi cliente, bypassa il blocco 'approvata non modificabile') + modale **"Scheda cliente"** nell'editor Diete del backoffice (nome/descrizione/caratteristiche/obiettivo/visibile). **Nessuna migrazione** (i campi esistono già). Backoffice type-check + build ok.

- **Notifiche push (FCM) — codice completo** — **Backend**: modello `PushToken` + migrazione (validata su PG locale), endpoint `POST/DELETE /me/push-tokens`, servizio `PushService` con **firebase-admin** che invia la push quando si crea una notifica (agganciato a `notifyOncePerDay` e `notify`, rispetta le preferenze). Legge le credenziali SOLO da `FIREBASE_SERVICE_ACCOUNT` (env su Render); se manca è **no-op** (in-app+email continuano). **App**: plugin `@capacitor/push-notifications`, modulo `lib/push.ts` (permesso → token → `POST /me/push-tokens`), agganciato dopo il login (no-op su web). Gradle Android già predisposto per google-services (si attiva ora che c'è `google-services.json`). Firebase: progetto **metabole-cea15**, app **app.metabole.client** registrata. **Resta**: (1) Simone genera il *service account* → env `FIREBASE_SERVICE_ACCOUNT` su Render; (2) `npm install` + `npx cap sync android` + rebuild APK. App build ok; type-check backend su Render.

- **Prodotti dinamici — Fase D (backoffice: scheda cliente sulle diete)** — l'editor Diete del backoffice ora, alla creazione, permette di impostare la **scheda cliente** del prodotto (nome mostrato, descrizione breve, caratteristiche una-per-riga, obiettivo dimagrimento/mantenimento, **visibile nello schermo 16**). Backend: campi aggiunti a `CreateDietDto`/`UpdateDietDto` e persistiti in `createDiet` (update li salva già via spread). `keto` aggiunto alle mappe stile del backoffice. Backoffice type-check + build ok. **Limiti noti (rifiniture successive):** non c'è ancora un form per **modificare** la scheda cliente di una dieta esistente (solo alla creazione); una dieta *approvata* non è modificabile (guard esistente), quindi per cambiarne la visibilità serve una via dedicata. Regole per-prodotto (Fase F) e agente (Fase E) ancora da fare.
- **Prodotti dinamici — Fase C (schermo 16 dinamico)** — lo schermo 16 "Stile che preferisci" ora legge i piani da `GET /onboarding/diet-products` (zero-redeploy): ogni nome è **toccabile** → apre le caratteristiche principali (descrizione + highlights); voce di Gaia **generica**; **Keto** al posto di Flessibile. Fallback statico se il catalogo è vuoto. Seed: i 4 prodotti (Mediterranea/Proteica/Low-carb/Keto) resi `clientVisible` (creati a menu vuoti se mancanti). Aggiunto `keto` ai validatori dietStyle (submit/profile/client/catalog) e a `styleNames`. App type-check + build ok; verifica visiva schermo 16 ok. NB: type-check backend reale su Render; contiene le migrazioni Fase A.
