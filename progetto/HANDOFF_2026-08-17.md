# Passaggio di consegne — Metabole, sera del 17/8/2026

Da incollare in cima a una chat nuova. Tutto quello che c'è qui è verificato nel codice o nei dati,
non ricordato.

---

## 0. Come si lavora qui (queste regole vengono prima di tutto)

- **Si verifica nel CODICE, non nei documenti.** Se un documento e il codice divergono, ha ragione
  il codice — e lo si dice. Oggi ho proposto due volte una causa senza aver guardato i dati, e due
  volte era sbagliata: una corsa fra richieste che erano a una settimana di distanza, e un piano da
  annullare che era l'altro.
- **Le decisioni si scrivono in un documento PRIMA del codice.** I fogli stanno in `progetto/`.
- **I test nuovi si vedono ROSSI prima.** E dove conta, si fa il **controllo per mutazione**: si
  disattiva la riga e si conta quanti test cadono. Un test che passa per caso non difende niente —
  oggi ne ho trovato uno che passava per l'ordine alfabetico.
- **Ogni consegna:** `COMMIT_parte_*.txt` + append a `progetto/COMMIT.txt` + voce in cima a
  `progetto/REGISTRO.md` + **Summary e Description in chat**, che la push la fa Simone.
- **File condivisi** (`schema.prisma`, `REGISTRO.md`, `COMMIT.txt`, `voci-iniziali.ts`): si
  rileggono dal Mac e non si sovrascrivono mai alla cieca.
- ⚠️ **MAI comandi git sulla cartella montata**: lascia `index.lock` non eliminabili.
- Niente archivi nel repo. Le migrazioni le applica Render al deploy (`render.yaml`
  `preDeployCommand`), **mai** dal Mac.

### ⚠️ La trappola che oggi è costata una CI rossa

La sandbox ha uno **stub di Prisma**: i modelli non ci sono e i tipi generati nemmeno. Quindi:

- `npx tsc --noEmit` in sandbox **non vale** sul codice che tocca Prisma;
- ~50 suite non compilano lì (`menu.service`, `cron.controller`, `commerce/*`, `catalog/*`…);
- l'host dei binari Prisma è **bloccato** (403), quindi il client non si può rigenerare.

**La verifica vera è `cd ~/Progetti/Metabole/backend && npm run build && npm test` sul Mac**, che è
esattamente quello che fa la CI. Quando si scrive «i tipi sono a posto» va detto *dove*.

### Come si consegna un file

Sandbox (`/root/metabole`, clone di `origin/main`) → si scrive → `SendUserFile` →
`mcp__remote-devices__device_commit_files` sul percorso reale
`/Users/simonesalogni/Progetti/Metabole/...` → **si verificano gli md5 dai due lati**.
Il path `~/mnt/Progetti--Metabole` è quello della VM di `device_bash`, non funziona per il commit.

---

## 1. Cosa gira adesso

**Il cron che riempie il catalogo** — `POST /internal/cron/genera-catalogo`, in `render.yaml` come
`metabole-cron-genera-catalogo`, ogni 10 minuti. Una settimana di catalogo per chiamata, priorità:
famiglie con clienti prima, dentro un gruppo la variante a 5 pasti prima (le altre due riusano le
sue ricette), settimane magre prima di quelle nuove.

- Simone ha detto: **lasciarlo andare senza fretta.**
- ⚠️ **È temporaneo**: si sospende quando il catalogo è pieno. Restano ~2000 unità di lavoro, ma la
  parte utile sono le prime ~430. **Quando la risposta smette di dire «N clienti su questa famiglia»
  e comincia a dire «nessun cliente sopra», il lavoro che serve è finito.**
- Le ricette nascono in bozza (`active: false`, allergeni da confermare): la nutrizionista le rivede
  al suo ritmo, ed è il motivo per cui questo si poteva accendere.

---

## 2. Consegnato oggi (tutto pushato, tranne dove indicato)

1. **Tre difetti dal caso Jolanda Todde** — un tag `"Carne .ceci"` che non escludeva niente (terza
   volta per quella riga: `latte` 8/8, `frutta_a_guscio` 12/8); Gaia che proponeva «ceci secchi» al
   posto di «ceci cotti»; Vera che non leggeva il nome a inizio frase.
2. **Il lato scrittura del tag** — `filtraSpezie` spezza prima di salvare; separatori in una sola
   definizione (`common/tag-alimenti.ts`).
3. **`npm run diag:digiuni`** e **`npm run diag:storia`** — due diagnostiche di sola lettura.
4. **Il cron del catalogo** (sopra), con il lucchetto anti-sovrapposizione.
5. **Vera: la via d'uscita da «su quale cliente?»** + la domanda che fa la pastiglia dei cambi.
6. **Annullamento abbonamento** — `POST /admin/subscriptions/:id/cancel` + pulsante × nella scheda.
   **Usato in produzione, funziona.**
7. **`sostitusci`** — la radice del verbo tollera i refusi.

---

## 3. ⚠️ Aperto, con una decisione di Simone in mezzo

### a) Sonia mangia un pasto al giorno — IL PIÙ URGENTE

`s.sandri66@libero.it`, finestra «salto la cena»: dovrebbe ricevere colazione, spuntino e pranzo,
**riceve il solo pranzo**. La variante `fasting` del catalogo ha tre slot fissi (pranzo, merenda,
cena) — è di fatto la variante «salta la colazione» — e la finestra toglie da lì.

La correzione è scritta e concordata a metà (Simone non ha mai detto né sì né no): **il motore deve
scegliere una dieta che abbia in catalogo i pasti che la finestra promette.** Chi salta la colazione
non si muove (100% delle kcal); chi salta la cena o il pranzo passa al catalogo a 5 pasti.
⚠️ Non «il digiuno usa sempre il 5 pasti»: sarebbe un peggioramento per le 5 clienti che oggi stanno
bene, che riceverebbero i pasti giusti con un terzo di calorie in meno, in silenzio.
Foglio: `progetto/NOTA_Digiuno_E_Riempimento_Varianti.md`.

### b) La seconda lettura di Vera — DECISIONE DA PRENDERE

`progetto/NOTA_Vera_Seconda_Lettura.md`. Oggi Vera si è rotta **tre volte** e tre volte ho aggiunto
un'espressione regolare. La proposta: quando `capisci` torna `null`, il modello **riscrive** la frase
nella forma canonica e la si rilegge col riconoscitore deterministico. Il modello non vede i dati,
non tocca il database, **non decide**: traduce. La riscrittura si mostra prima di eseguire.

**La domanda per Simone è in fondo al foglio, ed è una sola.**

### c) `queued` non è uno stato — la causa del caso Polidoro

`progetto/NOTA_Due_Piani_Attivi_Lorena.md`. Un piano in coda si scrive `active` con una data futura:
il database non può vietare due attivi, la scheda mostra due «Attivo» identici, la matita non sa che
sta disfacendo una coda (l'ha fatto 48 secondi dopo), e ⚠️ **`menu.service` fa
`findFirst({status:'active'})` e ne prende uno a caso** per decidere quando finisce il piano.
Dentro c'è anche: la matita che avvisa prima di sovrapporre, e «+ Attiva un piano» che si guarda
intorno come fa il questionario.
⚠️ Il vincolo in banca dati **non** va nella stessa consegna dello stato.

### d) Piccole, già diagnosticate

- Il pulsante × è `@Roles('admin')`: **da capo nutrizionista non si vede.** O si entra come admin, o
  gli si dà un permesso suo (`cancel_subscription`, come `change_plan_start`). ~30 minuti.
- Le due pastiglie di piano sono **identiche** (mostrano l'inizio, non la fine): si distinguono solo
  dal tooltip. La fine andrebbe scritta nella pastiglia.
- **Maria** (`mariabonaccorso@hotmail.it`) è in digiuno **senza finestra impostata**: nessuno gliel'ha
  mai chiesta. Non è un difetto del motore, è una domanda mancata.
- `menuDay.upsert` ha `update: {}`: un cibo non gradito dichiarato **dopo** non tocca i giorni già
  erogati, e nessuno lo dice né alla cliente né alla coach.
- Il backoffice scrive `dislikedFoods` senza passare da `filtraSpezie`.
- Voce 252 in elenco lavori: `allergensReviewed` va azzerato quando cambiano gli ingredienti dal
  backoffice? Tre strade, decide Simone.
- Sei righe da spuntare a mano nella pagina Lavori, e `CONFERMA=1 npm run carica:lavori` dopo un deploy.

---

## 4. I numeri di oggi, per non rifare le stesse domande

- **306 diete** in catalogo. Quelle con qualcuno sopra: **16**, per **25 clienti**, in **12 gruppi**
  di ricette. Tutti e dodici `dimagrimento`, undici su dodici `omnivore`: **metà della griglia dei 18
  non serve nessuno.**
- Le varianti con clienti hanno **28 giornate ma 19 piatti diversi per pasto** invece di 28: nate col
  metodo vecchio, che ricombinava pochi piatti su tante giornate.
- ⚠️ **Aggiungere giornate NON aggiunge varietà**: il motore non serve le giornate del catalogo così
  come sono, ne prende le ricette e ricompone (`dayComboPools`). Quello che una cliente percepisce è
  *quante ricette diverse ha ogni pasto*. Uno script che riempie giornate senza ricette nuove non
  darebbe niente a nessuno.
- **7 clienti in digiuno**: 5 su «salta colazione» (a posto), 1 su «salta cena» (Sonia, rotta), 1
  senza finestra (Maria).

---

## 5. Il difetto di famiglia di questo progetto

**«Un dato che agisce e non si vede.»** Pagato ormai sei volte: `menuAncoraSullaDietaPrecedente`,
`pastiEsclusi`, i traguardi della pesata, le allergie, il tag sporco, e i due piani attivi. Quando
qualcosa non torna, la prima domanda utile è quasi sempre: *questo dato cambia cosa mangia una
persona, e c'è una schermata che lo dice?*
