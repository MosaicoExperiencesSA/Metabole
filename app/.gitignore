node_modules
dist
dist-ssr
*.local

# Capacitor native (generati con `npx cap add`)
android
ios

# Firebase: chiave del client, va scaricata da Firebase e messa qui a mano.
# Vive solo in iCloud (non nel repo): la sua presenza attiva le push al build.
google-services.json

# build / cache
.vite
.DS_Store
*.log

# artefatti tsc -b
*.tsbuildinfo
vite.config.js
vite.config.d.ts
