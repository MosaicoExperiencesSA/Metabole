From 5f9c2383226c4b59f5054bda8ee97c7706fe5f6e Mon Sep 17 00:00:00 2001
From: Simone Salogni <simone.salogni@gmail.com>
Date: Sat, 18 Jul 2026 10:27:57 +0000
Subject: [PATCH 1/2] feat(app): X di chiusura nell'angolo dei bottom sheet
 (lista spesa e altri)

---
 app/src/components/Sheet.tsx |  3 +++
 app/src/theme.css            | 10 ++++++++++
 2 files changed, 13 insertions(+)

diff --git a/app/src/components/Sheet.tsx b/app/src/components/Sheet.tsx
index 488b60c..97f4ec0 100644
--- a/app/src/components/Sheet.tsx
+++ b/app/src/components/Sheet.tsx
@@ -5,6 +5,9 @@ export default function Sheet({ onClose, children }: { onClose: () => void; chil
   return (
     <div className="sheet-overlay" onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}>
       <div className="sheet-card">
+        <button className="sheet-close" onClick={onClose} aria-label="Chiudi">
+          <i className="ti ti-x" />
+        </button>
         <div className="sheet-grab" />
         {children}
       </div>
diff --git a/app/src/theme.css b/app/src/theme.css
index 4c6156e..b215809 100644
--- a/app/src/theme.css
+++ b/app/src/theme.css
@@ -341,6 +341,16 @@ p { line-height: 1.5; }
 }
 @keyframes sheetUp { from { transform: translateY(100%); } to { transform: translateY(0); } }
 .sheet-grab { width: 40px; height: 4px; background: #e2ded4; border-radius: 3px; margin: 0 auto 12px; }
+/* X di chiusura in alto a destra del pannello: resta visibile anche scrollando (sticky). */
+.sheet-close {
+  position: sticky; top: 0; z-index: 3;
+  margin-left: auto; margin-bottom: -18px;
+  display: flex; align-items: center; justify-content: center;
+  width: 34px; height: 34px; border-radius: 50%;
+  border: none; background: #F2EFE8; color: #5F6E6B;
+  font-size: 19px; line-height: 1; cursor: pointer;
+}
+.sheet-close:active { background: #E7E2D6; }
 
 /* popup umore */
 .mood-row { display: flex; justify-content: space-between; }
-- 
2.43.0


From 5b504a72c71ba1b361e70e2da5ccda0604271f60 Mon Sep 17 00:00:00 2001
From: Simone Salogni <simone.salogni@gmail.com>
Date: Sat, 18 Jul 2026 10:35:07 +0000
Subject: [PATCH 2/2] feat: reset password cliente (pagina app + trigger login)
 + pulsante nella mail

---
 app/src/App.tsx                 |   3 +
 app/src/pages/Login.tsx         |  13 ++--
 app/src/pages/ResetPassword.tsx | 118 ++++++++++++++++++++++++++++++++
 backend/src/i18n/messages.ts    |   4 +-
 progetto/REGISTRO_SVILUPPO.md   |   4 ++
 5 files changed, 131 insertions(+), 11 deletions(-)
 create mode 100644 app/src/pages/ResetPassword.tsx

diff --git a/app/src/App.tsx b/app/src/App.tsx
index 051d817..9314b70 100644
--- a/app/src/App.tsx
+++ b/app/src/App.tsx
@@ -11,6 +11,7 @@ import MeasuresGate from './components/MeasuresGate';
 import Landing from './pages/Landing';
 import Login from './pages/Login';
 import Register from './pages/Register';
+import ResetPassword from './pages/ResetPassword';
 import Diversi from './pages/Diversi';
 import Home from './pages/Home';
 import Menu from './pages/Menu';
@@ -69,6 +70,7 @@ function Shell() {
           <Route path="/checkout" element={<Checkout />} />
           <Route path="/payment/success" element={<PaymentResult ok />} />
           <Route path="/payment/cancelled" element={<PaymentResult ok={false} />} />
+          <Route path="/reset-password" element={<ResetPassword />} />
           <Route path="*" element={<Navigate to="/" replace />} />
         </Routes>
       </div>
@@ -139,6 +141,7 @@ export default function App() {
         <Route path="/diversi" element={<Diversi />} />
         <Route path="/login" element={<Login />} />
         <Route path="/register" element={<Register />} />
+        <Route path="/reset-password" element={<ResetPassword />} />
         <Route path="/conferma-email" element={<ConfermaEmail />} />
         <Route path="/verifica-email" element={<VerificaEmail />} />
         <Route path="*" element={<Navigate to="/" replace />} />
diff --git a/app/src/pages/Login.tsx b/app/src/pages/Login.tsx
index 54924ad..a53066d 100644
--- a/app/src/pages/Login.tsx
+++ b/app/src/pages/Login.tsx
@@ -17,7 +17,6 @@ export default function Login() {
   const [password, setPassword] = useState('');
   const [err, setErr] = useState<string | null>(null);
   const [busy, setBusy] = useState(false);
-  const [showHelp, setShowHelp] = useState(false);
 
   async function onSubmit(e: FormEvent) {
     e.preventDefault();
@@ -82,16 +81,12 @@ export default function Login() {
 
           <div
             className="muted"
-            style={{ textAlign: 'center', fontSize: 12, marginTop: 12, cursor: 'pointer' }}
-            onClick={() => setShowHelp((v) => !v)}
+            style={{ textAlign: 'center', fontSize: 12, marginTop: 12 }}
           >
-            Password dimenticata?
+            <span className="link" style={{ cursor: 'pointer' }} onClick={() => nav('/reset-password')}>
+              Password dimenticata?
+            </span>
           </div>
-          {showHelp && (
-            <p className="muted" style={{ textAlign: 'center', fontSize: 12, marginTop: 6 }}>
-              Scrivi a <a className="link" href="mailto:supporto@metabole.eu">supporto@metabole.eu</a> e ti aiutiamo a reimpostarla.
-            </p>
-          )}
 
           <p className="muted" style={{ textAlign: 'center', marginTop: 14, fontSize: 13 }}>
             Non hai un account? <Link className="link" to="/register">Registrati</Link>
diff --git a/app/src/pages/ResetPassword.tsx b/app/src/pages/ResetPassword.tsx
new file mode 100644
index 0000000..5945589
--- /dev/null
+++ b/app/src/pages/ResetPassword.tsx
@@ -0,0 +1,118 @@
+import { FormEvent, useState } from 'react';
+import { useNavigate, useSearchParams } from 'react-router-dom';
+import { apiPublic, ApiError } from '../api/client';
+import PasswordField from '../components/PasswordField';
+
+/**
+ * Reset password cliente — stesso flusso del backoffice.
+ * - Con ?token=… (dal link della mail): form per la NUOVA password → POST /auth/password-reset/confirm.
+ * - Senza token: form per richiedere il link via email → POST /auth/password-reset.
+ */
+export default function ResetPassword() {
+  const [params] = useSearchParams();
+  const navigate = useNavigate();
+  const token = (params.get('token') ?? '').trim();
+
+  const [email, setEmail] = useState('');
+  const [pw, setPw] = useState('');
+  const [pw2, setPw2] = useState('');
+  const [busy, setBusy] = useState(false);
+  const [err, setErr] = useState<string | null>(null);
+  const [done, setDone] = useState<null | 'requested' | 'reset'>(null);
+
+  async function requestLink(e: FormEvent) {
+    e.preventDefault();
+    setErr(null);
+    setBusy(true);
+    try {
+      await apiPublic('/auth/password-reset', { method: 'POST', body: JSON.stringify({ email: email.trim() }) });
+      setDone('requested');
+    } catch {
+      // Per privacy l'API risponde sempre 202: mostriamo comunque conferma.
+      setDone('requested');
+    } finally {
+      setBusy(false);
+    }
+  }
+
+  async function confirmReset(e: FormEvent) {
+    e.preventDefault();
+    setErr(null);
+    if (pw.length < 8) { setErr('La password deve avere almeno 8 caratteri.'); return; }
+    if (pw !== pw2) { setErr('Le due password non coincidono.'); return; }
+    setBusy(true);
+    try {
+      await apiPublic('/auth/password-reset/confirm', { method: 'POST', body: JSON.stringify({ token, newPassword: pw }) });
+      setDone('reset');
+    } catch (e) {
+      setErr(e instanceof ApiError ? e.message : 'Reset non riuscito. Il link potrebbe essere scaduto.');
+    } finally {
+      setBusy(false);
+    }
+  }
+
+  return (
+    <div className="app-frame">
+      <div className="screen no-tabbar" style={{ display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', padding: 24 }}>
+        <div className="card" style={{ width: '100%', maxWidth: 380 }}>
+          <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 14 }}>
+            <span style={{ width: 30, height: 30, borderRadius: '50%', background: 'var(--teal)', color: '#fff', display: 'inline-flex', alignItems: 'center', justifyContent: 'center', flex: 'none' }}>
+              <i className="ti ti-key" style={{ fontSize: 16 }} />
+            </span>
+            <b style={{ fontSize: 16 }}>Reimposta la password</b>
+          </div>
+
+          {err && <div className="banner err" style={{ marginBottom: 10 }}>{err}</div>}
+
+          {/* Esito */}
+          {done === 'reset' ? (
+            <>
+              <p className="muted" style={{ fontSize: 13 }}>Password aggiornata. Ora puoi accedere con la nuova password.</p>
+              <button className="btn" style={{ marginTop: 10, width: '100%' }} onClick={() => navigate('/login')}>Vai all'accesso</button>
+            </>
+          ) : done === 'requested' ? (
+            <>
+              <p className="muted" style={{ fontSize: 13 }}>
+                Se l'indirizzo è registrato, ti abbiamo inviato una email con il link per reimpostare la password. Controlla anche lo spam.
+              </p>
+              <button className="btn ghost" style={{ marginTop: 10, width: '100%' }} onClick={() => navigate('/login')}>Torna all'accesso</button>
+            </>
+          ) : token ? (
+            /* Con token → nuova password */
+            <form onSubmit={confirmReset}>
+              <div className="field">
+                <label>Nuova password</label>
+                <PasswordField autoComplete="new-password" value={pw} onChange={(e) => setPw(e.target.value)} minLength={8} required />
+              </div>
+              <div className="field">
+                <label>Conferma password</label>
+                <PasswordField autoComplete="new-password" value={pw2} onChange={(e) => setPw2(e.target.value)} minLength={8} required />
+              </div>
+              <button className="btn" type="submit" disabled={busy} style={{ marginTop: 6, width: '100%' }}>
+                {busy ? 'Salvo…' : 'Imposta la nuova password'}
+              </button>
+            </form>
+          ) : (
+            /* Senza token → richiesta link via email */
+            <form onSubmit={requestLink}>
+              <p className="muted" style={{ fontSize: 12, marginBottom: 10 }}>Inserisci la tua email: ti mandiamo il link per reimpostare la password.</p>
+              <div className="field">
+                <label>Email</label>
+                <input className="input" type="email" inputMode="email" autoComplete="username" value={email} onChange={(e) => setEmail(e.target.value)} required />
+              </div>
+              <button className="btn" type="submit" disabled={busy} style={{ marginTop: 6, width: '100%' }}>
+                {busy ? 'Invio…' : 'Invia il link di reset'}
+              </button>
+            </form>
+          )}
+
+          {!done && (
+            <p className="muted" style={{ textAlign: 'center', marginTop: 14, fontSize: 13 }}>
+              <span className="link" style={{ cursor: 'pointer' }} onClick={() => navigate('/login')}>Torna all'accesso</span>
+            </p>
+          )}
+        </div>
+      </div>
+    </div>
+  );
+}
diff --git a/backend/src/i18n/messages.ts b/backend/src/i18n/messages.ts
index 18357b6..f2dca0d 100644
--- a/backend/src/i18n/messages.ts
+++ b/backend/src/i18n/messages.ts
@@ -166,7 +166,7 @@ const it: Catalog = {
   'mail.reset.body': {
     title: '',
     variants: [
-      '<p>Hai chiesto di reimpostare la password del tuo account Metabole.</p><p>Usa questo codice: <code>{token}</code> (oppure il link: <a href="{link}">reimposta password</a>)</p><p>Il codice scade tra 1 ora. Se non l\'hai richiesto tu, ignora questa email: la password resta invariata.</p>',
+      '<p>Hai chiesto di reimpostare la password del tuo account Metabole.</p><p style="margin:20px 0"><a href="{link}" style="display:inline-block;background:#12A386;color:#ffffff;text-decoration:none;padding:12px 26px;border-radius:10px;font-weight:600;font-family:Arial,Helvetica,sans-serif">Reimposta la password</a></p><p style="font-size:13px;color:#5F6E6B">Se il pulsante non funziona, copia questo link nel browser:<br/><a href="{link}">{link}</a></p><p style="font-size:13px;color:#5F6E6B">Il link scade tra 1 ora. Se non l\'hai richiesto tu, ignora questa email: la password resta invariata.</p>',
     ],
   },
   'mail.credentials.subject': { title: '', variants: ['Metabole — le tue credenziali di accesso'] },
@@ -351,7 +351,7 @@ const en: Catalog = {
   'mail.reset.body': {
     title: '',
     variants: [
-      '<p>You asked to reset your Metabole account password.</p><p>Use this code: <code>{token}</code> (or the link: <a href="{link}">reset password</a>)</p><p>The code expires in 1 hour. If you didn\'t request it, ignore this email: your password stays unchanged.</p>',
+      '<p>You asked to reset your Metabole account password.</p><p style="margin:20px 0"><a href="{link}" style="display:inline-block;background:#12A386;color:#ffffff;text-decoration:none;padding:12px 26px;border-radius:10px;font-weight:600;font-family:Arial,Helvetica,sans-serif">Reset your password</a></p><p style="font-size:13px;color:#5F6E6B">If the button doesn\'t work, copy this link into your browser:<br/><a href="{link}">{link}</a></p><p style="font-size:13px;color:#5F6E6B">The link expires in 1 hour. If you didn\'t request it, ignore this email: your password stays unchanged.</p>',
     ],
   },
   'mail.bank.subject': { title: '', variants: ['Metabole — bank transfer details ({description})'] },
diff --git a/progetto/REGISTRO_SVILUPPO.md b/progetto/REGISTRO_SVILUPPO.md
index bab56db..7e7140e 100644
--- a/progetto/REGISTRO_SVILUPPO.md
+++ b/progetto/REGISTRO_SVILUPPO.md
@@ -7,6 +7,10 @@ Log delle modifiche di sviluppo fatte da Simone + Claude Cowork. Tenuto **separa
 
 ## 2026-07-18
 
+- **App + backend — reset password cliente completo (come nel backoffice) + pulsante nella mail** — segnalazione di Simone: quando il cliente chiede il reset password deve ricevere la mail col pulsante, come il flusso del backoffice. Prima il flusso lato cliente era **rotto**: nel login "Password dimenticata?" mostrava solo "scrivi a supporto@metabole.eu" (non inviava nulla) e **l'app non aveva la pagina `/reset-password`**, quindi anche il link della mail (che punta a `app.metabole.eu/reset-password`) portava a un 404. Il backend già inviava la mail (stesso metodo `requestPasswordReset` usato dal backoffice; per i clienti il link punta all'app). Aggiunto: (1) **pagina `/reset-password`** nell'app (`ResetPassword.tsx`, rotta sia da loggato che non) — con token dal link mostra il form nuova password → `POST /auth/password-reset/confirm`; senza token mostra il form per richiedere il link → `POST /auth/password-reset`; (2) login "Password dimenticata?" ora porta al flusso di reset invece del messaggio "scrivi a supporto"; (3) **mail di reset con PULSANTE** vero (anchor con stile inline, verde Metabole) invece del semplice link testuale, IT+EN, con link di ripiego sotto. Il flusso è ora identico a quello del backoffice. App tsc 0 errori; backend transpile pulito; nessuna migrazione. Parte app → nuova APK; il pulsante nella mail è attivo subito al deploy.
+
+- **App — X di chiusura nell'angolo dei bottom sheet** — segnalazione di Simone ("quando alzo la lista della spesa faccio fatica a richiuderla"). I pannelli a scomparsa (`Sheet`: lista della spesa, aiuto rapido, contatti, prenotazioni…) si chiudevano solo toccando fuori dal riquadro. Aggiunta una **X in alto a destra** del pannello (sticky, resta visibile anche scrollando) che chiude il foglio. Vale per tutti i bottom sheet che usano il componente `Sheet`. Solo CSS + un pulsante nel componente. App tsc 0 errori. Parte app → nuova APK.
+
 - **Backend — l'invio delle misure eroga SUBITO i prossimi 2 giorni (niente attesa del check-in)** — verifica su richiesta di Simone sull'utenza `sim1one.salogni@gmail.com`: risultavano erogati solo 2 giorni (17 e 18/07) e nessun giorno futuro, quindi il modulo "Menu futuri" era legittimamente vuoto. Causa: in `deliverIfEligible` il ciclo successivo si sbloccava SOLO quando il menu corrente era già "scaduto" (`ultimo_giorno >= oggi → return []`) **e** dopo il check-in del giorno. Nel giorno stesso in cui la cliente invia le misure (che è l'ultimo del ciclo) non veniva quindi erogato nulla: i prossimi giorni sarebbero arrivati solo l'indomani. Scelta prodotto di Simone: **l'invio delle misure deve far arrivare subito i prossimi 2 giorni**. Modifica: il blocco ora scatta solo se esiste già un menu per un giorno **futuro** (`ultimo_giorno > oggi`, per tenere un buffer e non generare cicli all'infinito); quando la cliente è all'ultimo giorno del ciclo (o è rimasta indietro), il ciclo successivo si sblocca con il **gate misure** (`cycleNeedsMeasure`) — **rimosso** il requisito del check-in giornaliero per l'erogazione. La lista della spesa, derivando dai `menuDay`, si aggiorna di conseguenza. Combinata con l'hook già attivo (all'`upsertMeasurement` chiamiamo `deliverIfEligible`), inviare le misure erogherà i prossimi 2 giorni all'istante. Test `menu.service.spec` aggiornati (nuovi casi: ultimo giorno + misura → eroga subito; ultimo giorno senza misura → held; buffer futuro → non eroga). Solo backend → live al deploy, nessuna APK. Nessuna migrazione.
 
 - **App — modulo "Menu futuri" in "Il tuo percorso"** — segnalazione di Simone ("dove vedo i menu dei prossimi due giorni?"). I giorni futuri già erogati (sbloccati dall'invio delle misure) erano visibili SOLO nella pagina Menu tramite i chip Oggi/Domani; nel Percorso si vedeva solo "IL MENU DI OGGI" + il diario dei menu passati. Aggiunto in `Percorso.tsx` un modulo **"Menu futuri"** subito sotto il menu di oggi: mostra una card per ciascun giorno con data futura (`date > oggi`, ordinati dal più vicino) con etichetta "domani/tra N giorni", numero di pasti, kcal totali e un'anteprima dei nomi dei piatti; toccando si apre la pagina Menu. Compare solo se ci sono giorni futuri erogati. Nessuna modifica al backend (i giorni futuri sono già nella risposta di `GET /me/menu`, filtrata su `visibleFrom`, non sulla data). App tsc 0 errori. Parte app → nuova APK.
-- 
2.43.0

